/**
 * Inserts a substring into a base string at the specified position.
 * If the base string is {@code null}, this method returns the string "null".
 * The index count starts at 0.
 *
 * @param s the base string where the insertion occurs
 * @param insert the string to be inserted into the base string
 * @param offset the position (starting from zero) where the insertion begins
 * @return the resulting string after the insertion, or the string "null" if the base is {@code null}
 */
public static String insert(String s, String insert, int offset) {
    ...
}